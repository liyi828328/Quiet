<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="perseverance.li.quiet.detail.WelfareDetailActivity">

    <include
        layout="@layout/toolbar_layout"
        android:fitsSystemWindows="true" />

    <android.support.v7.widget.CardView
        style="@style/CardViewStyle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/appbar_layout"
        android:layout_margin="@dimen/item_padding">

        <ImageView
            android:id="@+id/welfare_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true"
            android:background="@mipmap/img_default_gray" />

        <ProgressBar
            android:id="@+id/welfare_progressbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminateTint="@color/quiet_theme_color"
            android:indeterminateTintMode="src_atop" />

        <perseverance.li.quiet.widget.ArrowDownloadButton
            android:id="@+id/arrow_download_button"
            android:layout_width="@dimen/download_button_size"
            android:layout_height="@dimen/download_button_size"
            android:layout_gravity="center"
            android:visibility="gone" />

    </android.support.v7.widget.CardView>

</RelativeLayout>
